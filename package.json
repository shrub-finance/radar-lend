{
  "name": "radar-lend",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:shrub-finance/radar-lend.git",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace app dev",
    "build:app": "yarn workspace app build",
    "app": "yarn build:app && yarn workspace app start",
    "deploy": "yarn workspace programs deploy",
    "build": "yarn workspace programs build",
    "test": "yarn workspace programs test"
  },
  "resolutions": {
    "@solana/web3.js": "1.95.2"
  }
}
